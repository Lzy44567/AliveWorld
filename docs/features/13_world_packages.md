# 世界包与一键游玩

## 为什么现在做

AliveWorld 已经具备世界书、角色、文风、实体、因果账本、记忆、偏好、工坊和生图，但这些能力首先以“可自由配置的工具”出现。自由度越高，空白页负担越重；新玩家既不知道应该创建什么，也未必知道普通 AI 故事常见的平庸化、遗忘和世界停滞问题，因此无法仅凭功能名称理解 AliveWorld 的价值。

下一阶段不继续横向增加编辑器，而是把现有能力组织成可以导入、预览、安装和一键开玩的完整内容。首版不建设账号、评分、评论、审核或推荐算法；世界包先作为本地文件通过 QQ、Discord、GitHub Release 等渠道分享，等创作者和内容规模真实出现后再建设社区中心或 Steam 创意工坊。

## 玩家闭环

### 使用已有内容

1. 玩家打开“世界包”页，看到已安装世界和随包演示内容。
2. 选择一个世界包，阅读面向体验的简介，而不是先理解世界书、实体或账本术语。
3. 点击“一键开始”，系统从包内起点创建独立故事，并载入推荐资产与局内设置。
4. 玩家可以稍后进入高级资产栏检查或微调；不要求第一次开始前逐卡配置。

### 创作与分享

1. 创作者在现有工坊中完成世界书、角色、文风和实体。
2. 点击“创建世界包”，勾选要包含的资产；系统自动补全被引用的依赖。
3. 系统显示内容、缺失依赖、同名风险、成人标签、许可证和隐私扫描结果。
4. “导出世界包”只生成本地文件；“发布”属于未来社区动作，必须独立按钮和再次确认，不能把本地导出等同公开上传。

## 首版包结构

建议扩展名暂定为 `.aliveworld`，物理格式使用 ZIP，但玩家不需要手工解压。

```text
manifest.json
worldbooks/
characters/
styles/
entities/
images/
workflows/
starter/
```

`manifest.json` 至少包含：

- 稳定的 `package_id`、包版本、名称、作者和简介；
- 标签、成人内容标记、许可证与最低 AliveWorld 版本；
- 资产的稳定 ID、类型、相对路径、内容哈希和引用关系；
- 主世界书、默认角色、默认文风与可选实体组合；
- 可选故事起点、推荐局内设置和可选状态面板定义；
- 原始下载/更新来源；不保存作者本机绝对路径。

首版明确排除：

- API Key、模型连接密钥和账号信息；
- 玩家偏好卡、私人日志、已有游玩存档和记忆切片；
- 未经明确审计的可执行脚本、插件和二进制文件；
- 创作者 `UserData` 中未被选择的任何内容。

ComfyUI 工作流属于可选高级资源。导入时必须说明可能依赖外部模型和自定义节点，缺失时不阻止纯文字世界启动。

### dev.1 已冻结的技术契约

- YAML 资产首次参与打包时写入 `_aliveworld.asset_id`；重命名保留 ID，克隆必须生成新 ID。ID 不再依赖显示名称或文件名。
- `manifest.json` 使用 `schema_version: 1`，入口与依赖只引用包内稳定资产 ID；缺失依赖、循环依赖、重复 ID 和重复路径均拒绝导出或导入。
- 导出格式是确定性 ZIP：固定时间戳、规范 POSIX 相对路径、逐文件大小与 SHA-256。相同 `package_id + version` 但内容不同的包拒绝静默替换。
- 隐私扫描同时检查源路径、资产正文和 manifest；密钥、用户绝对路径、存档、日志、偏好、工坊草稿、模型目录和可执行文件不能进入首版世界包。
- 导入先完整检查路径穿越、文件数、大小、异常压缩比、清单一致性和哈希，再原子落盘到 `data/world_packages/installed/<package_id>/<version>/`。
- dev.1 导入只建立本地已安装内容与安装账本，不把包内卡片静默复制成个人全局资产，也不启动故事。冲突预览、实例化局内副本、一键开始和安全卸载属于 dev.2。
- 安装账本记录包哈希、manifest 路径、资产 ID、类型、原始哈希、落盘位置和修改标记；后续卸载只能依据账本与引用关系，不能按名称猜测所有权。

### dev.2 已实现的玩家闭环

- 游戏右侧默认入口调整为“开始与世界包”；没有安装内容时仍明确保留“空白故事”，不会强迫老玩家使用世界包。
- 导入前先显示名称、版本、内容数量和版本关系。不同来源的同名包允许并存但明确提醒；相同包 ID 与版本却内容不同则拒绝覆盖。
- “一键开始”把包内非可选世界书、角色、文风和实体复制到新故事自己的目录，再创建会话；故事保存 `world_package.json` 溯源，但以后不依赖安装目录继续运行。
- 起点 JSON 可以给出梗概、开场和推荐局内设置。玩家创建的故事仍使用完整现有游戏循环，不共享作者聊天记录。
- 默认安全卸载会把安装目录移入本机恢复区，并把玩家改过的资产另存为个人资产；已经创建的故事不会随包删除。另有“仅移除记录”和需要明确确认的彻底移除协议，玩家界面首版只开放安全卸载。
- 包详情会报告已修改与丢失的安装资产；一键开始前重新校验实际文件哈希，损坏内容不能进入新存档。

### dev.3 已实现：体验预设、身份与导入编排

用户于 2026-08-18 补充确认：世界包不仅携带资产，还应携带一份创作者建议的默认设置；玩家导入后应能直接创建存档、载入资产并进入故事。此补充会改变包契约与导入流程，因此在继续外部格式适配器前先完成以下设计与实现切片。

`v1.6.0-dev.3` 已完成受限体验预设、结构化来源、局内实例 ID、“安装并开始”组合接口、独立世界包管理入口，以及带关联故事清单的安全卸载。关联故事默认全部保留；“卸载时默认勾选”只控制本机复选框初态，最终确认不会省略。

#### 世界包体验预设

“默认设置”在玩家界面称为**体验预设**，避免让玩家误以为它会覆盖全部全局设置。体验预设只在从该包创建新故事时复制为局内设置，至少区分：

- `defaults`：创作者推荐值，玩家创建前和创建后都可修改；
- `visibility`：实体名称、动机、气泡、未来可能性等作品展示方式；
- `required_capabilities`：运行该世界确实需要的 AliveWorld 能力与最低版本；
- `locked_keys`：极少数会破坏作品规则的局内选项。锁定项必须在开始前逐项说明原因，不能隐藏。

世界包不得携带或锁定 API Key、接口地址、模型账号、日志/诊断权限、更新设置、文件路径与无障碍选项。包更新不得静默改变已经创建的存档设置。首版优先实现推荐默认值与展示预设；强制锁定属于后续能力，不能为了“创作者决定玩家看到什么”剥夺玩家的安全、隐私和可访问性控制。

#### 导入与开始是一个玩家流程、两个系统动作

面向普通玩家的主按钮使用“**安装并开始**”：

1. 预检包、依赖、版本关系、体验预设和冲突；
2. 让玩家填写存档名，并预览将载入的资产与设置；
3. 原子安装世界包、创建独立存档、实例化局内资产、写入溯源与体验预设；
4. 全部成功后才切换到新故事。任一步失败都不得留下半创建存档。

同时保留次级“**仅安装**”，供创作者、收藏者和稍后游玩的用户使用。安装世界包不等于创建存档；只是新手主路径不要求先理解这两个动作。一个已安装世界包可以反复创建多个互不影响的故事。

#### 名称、标签与身份分工

- 名称仅用于展示，允许不同作者、不同包和不同语言出现同名资产。
- 自由标签用于内容分类；系统可附加可读的“来自某世界包”来源标签，供搜索、筛选和 UI 分组。
- 标签**不得**作为更新、覆盖、依赖、卸载或所有权判断依据，因为标签可编辑、可重复，也可能被翻译。
- 技术身份继续使用稳定 `package_id`、`asset_id` 和结构化来源元数据。局内副本还要有独立 `local_instance_id`，并记录 `source_package_id`、`source_package_version` 与 `source_asset_id`。
- 包内依赖只引用稳定 ID。玩家给资产批量改显示名称，不得破坏包内关系。

#### 冲突分类与默认处置

系统先按身份与哈希分类，不能把所有冲突都叫“重名”：

| 情况 | 判断 | 默认处理 |
| --- | --- | --- |
| 同 `package_id`、同版本、同包哈希 | 已安装的同一版本 | 幂等复用，不重复创建安装资产 |
| 同 `package_id`、同版本、不同包哈希 | 非法版本碰撞或被篡改 | 阻断；创作者必须提升版本号 |
| 同 `package_id`、更高版本 | 同一世界包更新 | 显示变更摘要；不改写既有存档 |
| 同 `asset_id`、内容变化 | 同一逻辑资产的新版本 | 未修改源资产可升级；已修改副本保留并提示分叉 |
| 不同 ID、相同显示名称 | 只是名称相同 | 默认并存，以作者/包来源徽标区分 |
| 局内副本与来源相同但内容已修改 | 玩家分叉资产 | 永不静默覆盖，保留本地修改 |
| 新存档与已有存档同名 | 存档命名冲突 | 要求玩家改名或自动建议可读后缀 |

普通玩家使用“安装并开始”时，包资产默认只实例化为该存档的局内资产，因此不需要为全局资产重名承担复杂选择。把包资产提升为可复用的个人全局资产属于高级操作，届时提供：保留两者并追加来源后缀、批量重命名传入资产、逐项对比后替换。**批量重命名玩家已有资产不是默认方案**，因为它可能影响其他存档和引用；若以后实现，必须显示受影响范围并以事务方式更新引用。

#### 进一步预留

- 一个世界包可提供多个起点、玩家身份或难度预设，但共享同一稳定包身份；
- 安装源保持只读，玩家编辑的是局内副本或显式创建的个人分叉，便于安全更新与卸载；
- 包详情显示作者、包 ID 的短标识、版本、语言、内容分级、许可证、预计上下文成本和可选依赖；
- 未来可加入作者签名与可信来源，但本地未签名包仍可在明确警告后导入；
- 更新后的世界包默认用于新故事；既有故事是否迁移需要单独迁移协议，首版不自动迁移；
- 删除世界包时必须列出由它创建的故事，只移除安装源，不连带删除故事。

## 世界包不是存档

世界包描述“如何开始和运行一个世界”，存档描述“某位玩家已经玩到哪里”。一键开始必须从包生成新的独立故事，不能让多个玩家共享同一个可写存档目录。

首版可包含故事梗概、开场场景、推荐设置和初始状态，但不包含作者的聊天历史。以后若允许发布示例回放，也应作为只读演示资产单独标记。

## 安装记录与安全卸载

需要提供“一键卸载世界包”，但不能按名称粗暴删除所有同名文件。系统必须为每次安装建立安装账本，记录：

- `package_id` 与安装版本；
- 每个安装资产的稳定 ID、原始哈希和落盘位置；
- 资产是否被玩家修改；
- 是否被故事、其他世界包或局外资产引用；
- 世界包更新产生的版本关系。

卸载提供三个语义清晰的选项：

1. **安全卸载（默认）**：删除仍保持原始内容且没有外部引用的包资产；保留玩家修改过或仍被引用的资产，并将其转为个人资产。
2. **仅移除世界包记录**：保留全部已安装资产，只取消包的安装/更新关系。
3. **检查后移除关联资产**：显示逐项清单和影响范围后再删除；现有故事存档默认永不随包卸载删除。

多个包共享同一依赖时，只有最后一个引用解除后才允许删除。任何卸载都先生成可恢复清单；不能用文件名或显示名称作为唯一删除依据。

## 导入冲突与更新

- 相同 `package_id` 且版本更高：作为包更新，先显示变更摘要。
- 不同 `package_id` 但资产同名：默认并存并提示重命名，不静默覆盖。
- 相同资产 ID 且本地未修改：允许升级。
- 相同资产 ID 但本地已修改：保留本地版本，提供对比、另存副本或显式覆盖。
- 包更新不能重写由该包创建的玩家存档；存档持有自己的局内副本和状态。

## 与酒馆生态兼容

兼容层与 AliveWorld 内部格式分离。首版优先支持：

- Character Card V2/V3 的 PNG/JSON 导入；
- 卡内 `character_book` 与常见 Lorebook JSON；
- 角色基础字段、标签、首条消息、系统提示和附加图片的可解释映射；
- 无法映射的扩展字段保存在导入报告中，不假装已经生效。

酒馆角色卡可以成为世界包的一部分，但“导入一张角色卡”不自动生成暗流实体、因果账本或完整世界。需要由创作者或工坊明确升格和补全。

### dev.4 已实现：外部格式兼容边界

`v1.6.0-dev.4` 将外部格式解析放入独立 `core/external_formats/` 适配层，不让 Character Card 或 SillyTavern 字段渗入角色、世界书、暗流实体等内部领域模型。

- 支持 Character Card V2/V3 JSON，以及在 PNG/APNG `tEXt` / `zTXt` 块中携带 `chara` 或 `ccv3` 的角色卡；两者并存时按规范优先读取 `ccv3`。
- PNG 角色卡本身作为全局角色立绘保存；导入过程不访问卡内 URL，不执行扩展、脚本或正则表达式。
- 支持 `lorebook_v3`、Character Book 风格数组条目，以及常见 SillyTavern `entries` 对象、`key`、`keysecondary`、`disable`、`constant` 字段。
- 原生字段明确映射；性格、场景和对话示例降级合并进角色描述；Lorebook 的 `constant` 映射为 AliveWorld “常驻”标签。
- 系统提示、卡内 Lorebook、正则、选择性次关键词、扫描深度、插入顺序、V3 多媒体资源和群聊问候没有等价语义时，不会冒充已生效。完整来源保存在 `_external_import`，预览报告分别列出“已映射、降级、仅保留、警告”。
- 外部系统提示词不会自动取得 AliveWorld 系统权限；卡内 `character_book` 不会自动创建世界书或实体，避免一次普通角色导入静默改变整个故事。
- 上传限制 16 MB，JSON 解码上限 8 MB，PNG 文本块采用有界解压；同名个人资产返回冲突并要求改名，不允许静默覆盖。

首版是单向兼容导入，不承诺把修改后的 AliveWorld 资产无损导出回原格式；V3 CHARX、多资产下载、角色专属 Lorebook 运行时叠加和双向导出后置。

## UI 信息架构

不在世界包 MVP 前重写全部三栏界面。先调整首次入口和信息层级：

- **开始/世界包**：默认面向玩家，展示已安装内容、演示世界、继续故事和导入世界包。
- **游戏模式**：保持当前正文与局内资产三栏工作区。
- **工坊模式**：面向创作者，负责制作资产和导出世界包。
- **高级资产管理**：保留逐卡编辑、局内/全局作用域和调试能力，不在新手首屏展开。

世界包卡片首先回答“会获得什么体验”：角色是否具有长期行动、世界是否会在幕后变化、是否含生图/成人内容、故事规模和预计配置难度。不要用 Overseer、RAG、贝叶斯等内部术语作为卖点标题。

真正需要重排的是入口与渐进披露，不是立刻更换配色或重写所有卡片。等两套官方世界完成后，再以真实世界包内容检验首页、包详情和一键开始布局。

## 新手教程的职责

教程不要求玩家先学会创造世界。首个闭环只包含：

1. 配置并测试一个正文模型；
2. 选择演示世界包；
3. 一键创建故事并发送第一回合；
4. 认识“继续故事、查看日志、保存位置”三个入口。

角色、世界书、文风、实体、记忆和 ComfyUI 分别提供按需短教程。介绍 AliveWorld 价值时使用可观察结果，例如“角色会根据过去改变计划”“玩家未到场时世界仍会发展”，而不是只列模块名称。

## 首版验收

- 全新 `UserData` 可以导入一个世界包并在三分钟内开始第一回合；
- 包内资产依赖完整载入，重启后仍可继续；
- 相同包重复导入不会静默制造重复或覆盖修改；
- 安全卸载不删除玩家存档、玩家修改资产或其他包共享资产；
- 导出的包不含密钥、日志、私人存档、偏好和绝对路径；
- 无 ComfyUI 的电脑仍能运行含可选工作流的文字部分；
- 酒馆格式导入给出成功、降级和未支持字段报告；
- 自动验收覆盖导出、导入、一键开始、更新冲突、重启恢复和安全卸载。
- “安装并开始”在一次确认后完成安装、建档、资产实例化、体验预设应用和会话切换；失败不留下半创建存档。
- 两个不同来源但同名的包或资产可以安全并存；重命名显示名称不破坏依赖、更新与卸载。
- 世界包体验预设只影响由它新建的故事，不覆盖全局设置、密钥或既有存档。

## 版本切片

1. `v1.6-dev.1`：已冻结 manifest、资产 ID、依赖图、隐私边界和安装账本；本地导出/检查/原子导入领域内核完成。
2. `v1.6-dev.2`：已完成一键开始、包详情、版本/同名冲突预览和安全卸载，并接入玩家端最短路径。
3. `v1.6-dev.3`：已完成体验预设契约、结构化来源、冲突分类、“安装并开始”编排和世界包管理入口。
4. `v1.6-dev.4`：已完成 Character Card V2/V3、PNG 与 Lorebook 兼容适配器、字段保留和导入报告。
5. `v1.6-dev.5`：已完成工坊资产选择、本地 `.aliveworld` 导出及第一个官方小型演示世界“雾港回声”。导出只接受资产身份选择，不接受任意路径；官方演示仍走普通包的检查、安装、建档与溯源链路。
6. `v1.6-dev.6`：已完成第二个大型官方世界“烬环城：停炉前夜”；世界包起点新增受限的 `initial_state`，可初始化属性、状态条、状态效果和在场角色，但不能携带可执行代码或每回合自动逻辑。官方世界列表按推荐内容优先，并展示规模、时长、难度与体验亮点。自动验收覆盖一键建档、初始状态、四个暗流实体、Overseer 行动、因果账本及玩家端暗流提示。
7. 后置：全局资产提升与批量重命名、强制体验锁定、账号、远程发布、评分、评论、审核、推荐、Steam 创意工坊和托管社区。
