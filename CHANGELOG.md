# AliveWorld 更新日志 (Changelog)

## [v1.5.0-dev.9] - Windows 便携版构建链 - 2026-07-25

### 启动与打包
- 新增无控制台桌面启动器：自动选择可用端口、启动同源服务并等待健康检查；默认使用 Windows WebView2 承载完整游戏窗口，关闭窗口即退出后台，WebView2 不可用时才回退浏览器控制窗。
- Windows 下使用命名互斥量避免重复启动；再次双击会打开已经运行的 AliveWorld。
- 新增 PyInstaller `onedir` 规格、维护者一键构建脚本、版本化 ZIP 与 SHA-256 校验文件。
- 构建只收集 Git 已跟踪的 `*.template.yml/json`，不会把本机个人角色卡、存档、图片或工坊草稿打进公开发行包。
- 新增端口冲突和重复启动辅助逻辑测试。
- 修复 Windows PowerShell 5.1 对 UTF-8 无 BOM 构建脚本的误读；批处理入口与发行文件名统一使用 ASCII。
- 修复无控制台 EXE 中 Uvicorn 默认日志格式器访问空 `stdout.isatty()` 导致启动即退出的问题。
- 替换脚手架遗留的页面标题与 favicon，使用独立 AliveWorld 图标文件名规避 localhost 浏览器缓存。
- WebView2 窗口先显示 AliveWorld 加载页，再在后台导入后端并切换至游戏，避免双击后长时间无反馈。
- 首次便携版启动可发现发行包上级目录中的旧源码数据，并经玩家确认后复制存档与个人资产；不删除旧文件、不覆盖冲突文件、不迁移模型缓存。

## [v1.5.0-dev.8] - 便携版运行底座 - 2026-07-25

### 发行工程
- 新增单一 `VERSION` 版本源，FastAPI、前端界面与 npm 包统一显示 `1.5.0-dev.8`。
- FastAPI 可在同一端口托管生产前端、静态资源、SPA 路由与 `/api`，玩家发行包不再需要 Node.js。
- 前端 API 全面改为同源相对地址；Vite 开发模式继续通过代理连接本地后端。
- 新增源码版/打包版运行路径层：源码开发保留现有目录，打包版将配置、存档、资产、图片、日志与工坊草稿隔离到 `%LOCALAPPDATA%\AliveWorld`。
- 首次运行仅复制明确标记的模板文件和安全配置示例，不复制个人资产，也不覆盖用户已编辑内容。

### 本地安全与回归
- 取消默认全来源 CORS；只有显式配置的开发来源才允许跨域。
- 系统配置接口不再向浏览器回传 API Key，浏览器本地存储也不再保存三类模型密钥；留空保存会保留磁盘中已有密钥。
- 新增路径隔离、模板初始化、单端口前端服务、SPA 回退、CORS 边界和密钥保护回归测试。

## [v1.0.0] - 创世正式版 (Formal Release) - 2026-06-XX
*经过了漫长的 MVP 概念验证与底层重构，AliveWorld 迎来了真正的 V1.0.0 正式版。此版本彻底抛弃了早期硬编码的桎梏，确立了“高自由度动态沙盒”与“暗流双轨推演”的终极架构。*

### 🚀 史诗级核心重构
- **动态物理沙盒引擎**：彻底剥离了死板的 HP/MP 等硬编码属性。一切数值、进度条均由 AI 动态接管并下发，完美支持修仙、赛博、废土等无限世界观的自由切换。
- **暗流实体真实降临**：重写了 `UndercurrentEngine`。 Overseer（世界意志）推演出的暗中实体、势力变化，将直接实时以 `.yml` 物理文件的形式写入本局沙盒存档，真正实现“万物皆有因果，实体皆可追溯”。
- **Vue3 模块化重构**：前端代码彻底告别“屎山”。解耦拆分了雷达面板、状态条渲染、多重弹窗管理，引入了极致的色彩智能自适应系统，UI 表现力与拓展性产生质的飞跃。

### ✨ 新增功能特性
- **双向资产流通体系**：实现了“全局万象图鉴”与“局内专属设定”的物理隔离与双向另存为机制。玩家可随时向世界中拖入新角色、世界书，或将本局衍化出的极品 NPC 推送回全局图鉴。
- **命运观测与完美重掷**：新增 N*n 未来观测包面板。接入独立的 `/reroll` 接口，支持玩家在不满意命运分支时，通过强制排异算法，平滑地重选一条全新的时间线。
- **智能 Buff 与环境解析**：全面支持基于回合制的持续状态结算（如中毒、自愈）。前端新增极具沉浸感的高亮色标签，能够智能识别 NPC 阵营并赋予相应的色彩表现。

### 🔧 体验优化与修复
- 实现了前后端大模型 API 配置的实时热重载。
- 修复了回溯记忆时，物理沙盒实体文件未能同步回档的“幽灵实体”灾难性 Bug。
- 增加了极其醒目的时间流逝置顶显示，并支持在局内设置中自由开关环境感知 UI。
